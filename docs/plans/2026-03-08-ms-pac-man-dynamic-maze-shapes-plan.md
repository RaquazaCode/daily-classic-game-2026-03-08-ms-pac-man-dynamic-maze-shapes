# Implementation Plan

1. Create deterministic game state with two maze phases and seed-based ghost routing.
2. Implement input, movement rules, collision handling, scoring, win/lose checks, and reset/restart.
3. Add canvas renderer, HUD sync, and browser hooks.
4. Run tests/build/capture and record artifacts.
